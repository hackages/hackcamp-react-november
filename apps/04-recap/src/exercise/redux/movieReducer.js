const SAVE_ALL_MOVIES = 'SAVE_ALL_MOVIES';

/* 
  The movieReducer should only handle the movies
  the state is described by the initialState variable
    - movies is a list of movies
  It should have only one action creator:
    - saveAllMovies: it should take a list of movies as parameter
*/

const defaultState = {
  movies: []
};

export const movieReducer = (state, action) => {
  return null;
};
