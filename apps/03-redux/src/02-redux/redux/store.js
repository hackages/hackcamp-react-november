import {createStore} from 'redux';

// TODO: create the rootReducer
const rootReducer = null;

// TODO: create a store and pass it your reducer. You'll need to import "createStore" from redux for that
export const store = null;
